# local arm64 virtualenv (1.5G — never commit)
.venv/

# datasets (12G — download separately, see README)
Datasets/

# macOS
.DS_Store

# generated bytecode
# NOTE: upstream committed some cpython-36 .pyc files; those stay tracked.
__pycache__/
*.cpython-311.pyc

# local session handoff notes (not for the fork)
PORT_HANDOFF.md
